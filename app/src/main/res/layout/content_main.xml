<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.vetealinfierno.punchcard.MainActivity"
    tools:showIn="@layout/app_bar_main">

<!-- Clock In -->
    <Button
        android:id="@+id/btn_clockIn"
        style="@android:style/Widget.Holo.Button.Toggle"
        android:layout_width="200dp"
        android:layout_height="75dp"
        android:layout_marginLeft="92dp"
        android:layout_marginTop="42dp"
        android:onClick="onClickClockIn"
        android:text="Clock In"
        android:textAppearance="@style/TextAppearance.AppCompat.Button"
        android:textSize="30dp"
        android:theme="@android:style/Widget.Holo.Button.Toggle"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

<!-- Clock Out -->
    <Button
        android:id="@+id/btn_clockOut"
        style="@android:style/Widget.Holo.Button.Toggle"
        android:layout_width="200dp"
        android:layout_height="75dp"
        android:layout_marginLeft="92dp"
        android:layout_marginTop="58dp"
        android:onClick="onClickClockOut"
        android:text="Clock Out"
        android:textAppearance="@style/TextAppearance.AppCompat.Button"
        android:textSize="30dp"
        android:theme="@android:style/Widget.Holo.Light.Button.Toggle"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/btn_clockIn" />

</android.support.constraint.ConstraintLayout>
