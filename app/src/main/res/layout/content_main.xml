<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.vetealinfierno.punchcard.MainActivity"
    tools:showIn="@layout/app_bar_main">

<!-- Clock In -->
    <Button
        android:id="@+id/btn_clockInOutBtn"
        style="@android:style/Widget.Holo.Button.Toggle"
        android:layout_width="200dp"
        android:layout_height="75dp"
        android:layout_marginLeft="90dp"
        android:layout_marginStart="90dp"
        android:layout_marginTop="48dp"
        android:onClick="onClickClkInOutBtn"
        android:text="clkInOutBtn"
        android:textAppearance="@style/TextAppearance.AppCompat.Button"
        android:textSize="25dp"
        android:theme="@android:style/Widget.Holo.Button.Toggle"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

<!-- Clock Out -->
    <Button
        android:id="@+id/btn_breakBtn"
        style="@android:style/Widget.Holo.Button.Toggle"
        android:layout_width="200dp"
        android:layout_height="75dp"
        android:layout_marginLeft="92dp"
        android:layout_marginStart="92dp"
        android:layout_marginTop="56dp"
        android:onClick="onClickBrkBtn"
        android:text="brkBtn"
        android:textAppearance="@style/TextAppearance.AppCompat.Button"
        android:textSize="25dp"
        android:theme="@android:style/Widget.Holo.Light.Button.Toggle"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/btn_clockInOutBtn" />

</android.support.constraint.ConstraintLayout>
